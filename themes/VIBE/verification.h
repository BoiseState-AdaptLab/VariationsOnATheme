#include<iostream>
#include<stdlib.h>

int collect_verification_statistics(unsigned char* map_gpu,
                                    unsigned char* map_serial,
                                    int rows,int cols);

